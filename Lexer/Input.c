#include <stdio.h> //Single line comment
#define SUB - 
#define ADD + // Define alias

int main() {
    int a;
    scanf("%d",&a);
    double f=0+ a;
    /*
    This is a multiline comment
    that will
    cover
    multiple lines */
    result *= f+89; // Store result
    $xy#d= result%45;
    56ab?cd+= 89.56;
    for(int i=9;i!=0;i--) {
    	if(i^4!=5)
    		i>>2;

    }
    printf("The result is %d", result);
}